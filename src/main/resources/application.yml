spring:
  datasource:
    username: root
    password: test
    url: jdbc:mysql://localhost:3306/plan_admin
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true